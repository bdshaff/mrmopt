# Create a formatted table of model parameters

Create a formatted table of model parameters

## Usage

``` r
mrm_params_table(mrm, scaled = TRUE)
```

## Arguments

- mrm:

  A model object.

- scaled:

  Logical indicating whether to return scaled parameters. Default is
  TRUE.

## Value

A gt table of model parameters.
