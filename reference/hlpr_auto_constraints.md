# Auto-generate constraints from model return rate ranges

Auto-generate constraints from model return rate ranges

## Usage

``` r
hlpr_auto_constraints(mrms, weekly_budget, bounds_multiplier = 3)
```
