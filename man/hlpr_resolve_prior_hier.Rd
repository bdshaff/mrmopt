% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hlpr_resolve_prior_hier.R
\name{hlpr_resolve_prior_hier}
\alias{hlpr_resolve_prior_hier}
\title{Resolve an mrm_prior specification into a hierarchical brms prior object}
\usage{
hlpr_resolve_prior_hier(
  mrm_prior = NULL,
  scaled_data,
  x,
  y,
  scale_method,
  scale_values,
  type,
  group,
  pool = c("b", "e", "d"),
  group_sd_prior = NULL
)
}
\arguments{
\item{mrm_prior}{An object of class \code{mrmopt_prior}, or \code{NULL} for
package defaults.}

\item{scaled_data}{The scaled data frame.}

\item{x}{Name of the x column.}

\item{y}{Name of the y column.}

\item{scale_method}{Either \code{"min_max"} or \code{"std"}.}

\item{scale_values}{List of scaling parameters (min/max or mean/sd values).}

\item{type}{The response form type (e.g., \code{"gompertz"}, \code{"log_logistic"}).}

\item{group}{A character vector of grouping column names, ordered outermost
to innermost (the same vector passed to
\code{\link{hlpr_define_response_form_hier}}).}

\item{pool}{A character vector naming which of \code{b}, \code{c}, \code{d},
\code{e} receive group-level effects.}

\item{group_sd_prior}{An optional single \code{brms} distribution string
(e.g. \code{"exponential(1)"}) applied to every partial-pooling SD. When
\code{NULL} (default), per-parameter defaults are used: a wider prior on
the scale parameter \code{d} and a moderate prior on the shape parameters.}
}
\value{
A \code{brmsprior} object containing both population-level and
  group-level SD priors.
}
\description{
Extends \code{\link{hlpr_resolve_prior}} for hierarchical fits. Population
(channel-level) priors on \code{b}/\code{c}/\code{d}/\code{e} are produced by
\code{\link{hlpr_resolve_prior}} unchanged. In addition, partial-pooling
standard-deviation priors (\code{class = "sd"}) are appended for each pooled
parameter at each level of the hierarchy.
}
\details{
SD priors are specified in scaled parameter space, consistent with the
population priors. The defaults are weakly informative:
\itemize{
  \item shape parameters (\code{b}, \code{e}): \code{exponential(1)}
  \item scale parameter (\code{d}): \code{exponential(0.5)} — wider, so the
    ceiling can vary substantially across units to reflect size differences.
}
}
\seealso{
\code{\link{hlpr_resolve_prior}}, \code{\link{fit_response_hier}}
}
\keyword{internal}
